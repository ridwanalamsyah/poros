{
  "name": "velvet-collapse-sanity-proxy",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "wrangler": "^3.80.0"
  }
}
